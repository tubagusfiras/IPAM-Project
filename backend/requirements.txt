# IPAM Backend Dependencies
# Usage: pip install -r requirements.txt
fastapi
uvicorn[standard]
asyncpg
python-dotenv
python-multipart
openpyxl
weasyprint
redis[hiredis]
bcrypt
pyjwt
slowapi
prometheus-client
loguru
aiohttp[speedups]
# Testing (kept in the same image for simplicity — run pytest directly, no extra install step)
pytest
pytest-asyncio
httpx
time_machine
ruff
